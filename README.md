### Starting the Server
1. Navigate to the `server` directory in your terminal.
2. Ensure Python 3 is installed on your machine.
3. Install the dependencies using `pip install pyautogui`.
4. Run the server by executing: `python server.py`.

This will start the server, which listens for commands from the Android app via port 5000.
